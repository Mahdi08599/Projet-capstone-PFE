# =============================================================
# Capstone SSL Fraud Detection — Dependencies
# =============================================================
# Install with: pip install -r requirements.txt
# Tested with Python 3.10 / 3.11
# =============================================================

# --- Core data manipulation ---
pandas>=2.1.0
numpy>=1.24.0
pyarrow>=14.0.0           # fast parquet read/write

# --- Machine learning & preprocessing ---
scikit-learn>=1.3.0

# --- Deep learning (Self-Supervised Learning model) ---
torch>=2.1.0              # PyTorch for SSL encoder
# torchvision              # not needed (tabular data only)

# --- Visualization ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Experiment tracking (optional but recommended) ---
# mlflow>=2.8.0            # uncomment if you want experiment tracking
# tensorboard>=2.15.0      # uncomment for training curve visualization

# --- Notebooks ---
jupyter>=1.0.0
ipykernel>=6.25.0

# --- Utilities ---
tqdm>=4.66.0               # progress bars
joblib>=1.3.0              # parallel processing / model serialization
